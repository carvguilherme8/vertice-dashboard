# Escopo do repo: o dashboard, o que ele precisa pra rodar (data/processed,
# tests, scripts) e os notebooks de análise (notebooks_analises/, versionados
# como documentação — o notebook 01 só roda de novo com "2.Data Room/" local).
# O resto do material bruto do case (CSVs, v3/v4, outputs) fica só local, fora
# do controle de versão — por decisão explícita.
2.Data Room/
outputs/
v3/
v4/
Case Vértice 1.html
Projeto_Vertice.ipynb
Aula07_Pratica.ipynb

# artefatos de build/execução
__pycache__/
*.pyc
.pytest_cache/
*.log
data/logs/

# ferramentas locais
.claude/

# segredos — nunca versionados (chave da API Sandbox EloAgents fica só aqui)
**/.streamlit/secrets.toml
